#!/bin/bash

if [ $# -lt 1 ] ; then
    echo "requires a single argument!"
    exit -1
fi

# find ${1} -exec chown :www-data {} \;
find ${1} -exec chmod go-rwx {} \;
find ${1} -type d -exec chmod go+x {} \;
find ${1} -type f -exec chmod go+r {} \;
